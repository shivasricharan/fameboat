import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Metro TV Telugu — Case Study Snapshot | Fameboat',
  description: 'One-page summary: how Fameboat built a digital presence system for Metro TV Telugu in 7 days.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
